# CUSTOMIZE: Point to your deployed Go API in production builds.
# Leave empty for same-origin /api (Vite dev proxy or reverse proxy in production).
# Use http://localhost:8080 if calling the API directly without a proxy.
VITE_API_BASE_URL=
