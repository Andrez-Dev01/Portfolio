/**
 * Root application shell.
 * Featured Deployments UI is added in cursor/featured-deployments-ui-bffa.
 */
import styles from './App.module.css'

function App() {
  return (
    <div className={styles.app}>
      <main className={styles.placeholder}>
        <h1>Portfolio Frontend</h1>
        <p>React + Vite scaffold ready. UI components load on the featured-deployments branch.</p>
      </main>
    </div>
  )
}

export default App
